<Page x:Class="NguyenHoangDungWPF.Views.CustomerPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:vm="clr-namespace:NguyenHoangDungWPF.ViewModels"
      Title="CustomerPage">

    <Page.DataContext>
        <vm:CustomerViewModel />
    </Page.DataContext>

    <Grid Margin="20">
        <StackPanel>
            <Button Content="Tải danh sách" Command="{Binding LoadCommand}" Width="150" Margin="0,0,0,10"/>
            <DataGrid ItemsSource="{Binding Customers}" SelectedItem="{Binding SelectedCustomer, Mode=TwoWay}" AutoGenerateColumns="False" Height="300">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding CustomerID}" Width="50"/>
                    <DataGridTextColumn Header="Tên công ty" Binding="{Binding CompanyName}" Width="200"/>
                    <DataGridTextColumn Header="Người liên hệ" Binding="{Binding ContactName}" Width="150"/>
                    <DataGridTextColumn Header="Điện thoại" Binding="{Binding Phone}" Width="120"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Margin="132,330,132,0">
            <Button Content="Thêm" Width="80" Command="{Binding AddCommand}" Margin="0,0,10,0" Height="21" RenderTransformOrigin="0.5,0.5">
                <Button.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform ScaleY="1"/>
                        <SkewTransform/>
                        <RotateTransform/>
                        <TranslateTransform/>
                    </TransformGroup>
                </Button.RenderTransform>
            </Button>
            <Button Content="Sửa" Width="80" Command="{Binding EditCommand}" CommandParameter="{Binding SelectedCustomer}" Margin="0,0,10,0" Height="21"/>
            <Button Content="Xoá" Width="85" Command="{Binding DeleteCommand}" CommandParameter="{Binding SelectedCustomer}" Height="22"/>
        </StackPanel>
    </Grid>
</Page>
