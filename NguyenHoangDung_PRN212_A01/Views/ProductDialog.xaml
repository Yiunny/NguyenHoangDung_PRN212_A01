<Window x:Class="NguyenHoangDungWPF.Views.ProductDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Thông tin sản phẩm" Height="300" Width="400"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Tên sản phẩm:"/>
        <TextBox Grid.Row="0" Margin="0,20,0,0" Text="{Binding ProductName}"/>

        <TextBlock Grid.Row="1" Text="Giá:"/>
        <TextBox Grid.Row="1" Margin="0,20,0,0" Text="{Binding UnitPrice}"/>

        <TextBlock Grid.Row="2" Text="Còn trong kho:"/>

        <Button Grid.Row="3" Content="Lưu" Width="100" Height="35" HorizontalAlignment="Center" VerticalAlignment="Center"
                Click="Save_Click"/>
    </Grid>
</Window>
